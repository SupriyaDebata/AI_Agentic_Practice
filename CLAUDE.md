# Project Rules

## Git Push — Never push without explicit permission
Never run `git push` (or any variant: `--force`, `-u`, etc.) without asking the user first and receiving an explicit "yes" for that specific push. A prior approval does not carry over to future pushes.
